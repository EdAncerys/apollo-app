import { AuthProvider, useAuthDispatch, useAuthState } from './context';
import { logIn, signUp, getPosts, getOnePost } from './actions';

export {
  AuthProvider,
  useAuthDispatch,
  useAuthState,
  logIn,
  signUp,
  getPosts,
  getOnePost,
};
